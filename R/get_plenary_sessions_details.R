# Work-in-progress function for plenary session details
# x <- "https://www.parlament.gv.at/gegenstand/XX/NRSITZ/182?json=TRUE"


get_plenary_sessions_details <- function(session_id) {
    # li_res <- x %>% jsonlite::fromJSON()
    # Interactive JSON viewer for development (requires listviewer package):
    # listviewer::jsonedit(li_res)

    # history <- li_res$content$stage
    # class(history)
    #
    # history[[1]] %>%
    # tidyr::unnest_wider(names) %>%
    # tidyr::unnest(reden, names_sep="_") %>%
    # tidyr::unnest_wider(reden_data)
    #
    # class(li_res)
    NULL
}
