% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_events.R
\name{get_event_api_request}
\alias{get_event_api_request}
\title{Retrieve Event Data from the Parliamentary API}
\usage{
get_event_api_request(body_params, search_string)
}
\arguments{
\item{body_params}{A JSON formatted string containing the body parameters needed for the API call.}

\item{search_string}{A string used to filter the events based on the search term.}
}
\value{
A response object containing aggregated event data from multiple API calls.
}
\description{
Constructs and executes a POST API request to fetch event data from the
Austrian Parliament's filter endpoint. The function handles pagination by
iteratively fetching additional pages until a page is returned with fewer entries
than the specified pagesize.
}
\details{
The API request is constructed using a series of pipe operators to set the method,
query parameters, headers, and body. The request leverages the \code{httr2} package to perform
the call and manage verbose settings. Pagination is implemented via the function
\code{req_perform_iterative}, which continues to fetch data until a page returns less than
1000 entries (as checked by the helper function \code{is_complete}).
}
\examples{
\dontrun{
  # Example body parameters and search string
  body <- '{"key1": "value1", "key2": "value2"}'
  search <- "Plenarsitzung"

  # Retrieve event data
  response <- get_event_api_request(body, search)

  # Process the response as needed
  print(response)
}
}
