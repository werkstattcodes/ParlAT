% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_plenary_meeting_details.R
\name{get_plenary_meeting_details}
\alias{get_plenary_meeting_details}
\title{Get Details of a Plenary Meeting}
\usage{
get_plenary_meeting_details(
  url = NULL,
  institution = NULL,
  legis_period = NULL,
  meeting_number = NULL,
  details_on = NULL,
  echo = FALSE
)
}
\arguments{
\item{url}{Character or NULL. URL of the plenary meeting page on
\code{parlament.gv.at}. Can be an absolute URL (with or without a
\verb{?selectedStage=} query parameter) or a relative path. The
\code{selectedStage} parameter is ignored when fetching data — both
\code{selectedStage=100} and \code{selectedStage=110} return the same embedded
dataset. Mutually exclusive with \code{institution}, \code{legis_period}, and
\code{meeting_number}.}

\item{institution}{Character or NULL. Parliamentary chamber: \code{"NR"}
(Nationalrat) or \code{"BR"} (Bundesrat). Mutually exclusive with \code{url}.}

\item{legis_period}{Character, numeric, or NULL. Legislative period.
Accepts numeric (\code{28}), Arabic character (\code{"28"}), or Roman numeral
(\code{"XXVIII"}) formats. Mutually exclusive with \code{url}.}

\item{meeting_number}{Character, numeric, or NULL. Meeting number within
the legislative period (e.g. \code{50} or \code{"50"}). Mutually exclusive with
\code{url}.}

\item{details_on}{Character or NULL. Specifies which part of the meeting
data to return. If \code{NULL} (default), returns a 1-row tibble with meeting
metadata. Use \code{"speakers"} to return a multi-row tibble with one row per
speech, including timing information. Use \code{"decisions"} to return a
multi-row tibble with one row per agenda item (TOP). Use \code{"timeline"} to
return a multi-row tibble with one row per Sitzungsverlauf event.}

\item{echo}{Logical. If \code{TRUE}, prints the URL being fetched and the number
of rows returned. Default is \code{FALSE}.}
}
\value{
A tibble. The structure depends on \code{details_on}:

If \code{details_on = NULL} (default):
\itemize{
\item \code{meeting_url} (character): The URL used to fetch the data.
\item \code{meeting_title} (character): Full title of the meeting.
\item \code{meeting_citation} (character): Citation reference (e.g. \code{"50/NRSITZ"}).
\item \code{legis_period} (character): Legislative period code (e.g. \code{"XXVIII"}).
\item \code{meeting_type} (character): Meeting type label (e.g. \code{"Plenarsitzung"}).
\item \code{meeting_type_short} (character): Short type code (e.g. \code{"NRSITZ"}).
\item \code{meeting_nr} (integer): Meeting number within the legislative period.
\item \code{date} (Date): Date of the meeting.
\item \code{state} (character): Completion state (e.g. \code{"fertig"}).
\item \code{start_time} (POSIXct): Start time of the meeting.
\item \code{end_time} (POSIXct): End time of the meeting.
}

If \code{details_on = "speakers"}:
\itemize{
\item \code{meeting_url} (character): The URL used to fetch the data.
\item \code{meeting_title} (character): Full title of the meeting.
\item \code{meeting_citation} (character): Citation reference (e.g. \code{"50/NRSITZ"}).
\item \code{legis_period} (character): Legislative period code (e.g. \code{"XXVIII"}).
\item \code{meeting_type} (character): Meeting type label (e.g. \code{"Plenarsitzung"}).
\item \code{debate_id} (integer): Internal debate identifier.
\item \code{debate_type} (character): Debate type code (\code{"AS"}, \code{"ND"}, \code{"DA"}, \code{"SU"}).
\item \code{debate_typetext} (character): Human-readable debate type label.
\item \code{debate_text} (character): Full debate heading text.
\item \code{debate_starttime} (character): Debate start time (ISO 8601 or time string).
\item \code{debate_endtime} (character): Debate end time (time string).
\item \code{debate_limit} (integer): Default per-speech time limit in minutes.
\item \code{debate_state} (character): Debate completion state.
\item \code{speech_nr} (integer): Sequential speech number within the debate.
\item \code{speech_state} (character): Speech completion state.
\item \code{speaker_name} (character): Speaker name with party abbreviation.
\item \code{pad_intern} (integer): Internal person identifier.
\item \code{wm_type} (character): Speech type abbreviation (\code{"wm"}, \code{"un"}, \code{"sr"}, etc.).
\item \code{start_time} (character): Speech start time (HH:MM).
\item \code{duration} (character): Actual speech duration (MM:SS).
\item \code{speech_limit} (integer): Individual speech time limit in minutes.
}

If \code{details_on = "decisions"}:
\itemize{
\item \code{meeting_url} (character): The URL used to fetch the data.
\item \code{meeting_title} (character): Full title of the meeting.
\item \code{meeting_citation} (character): Citation reference (e.g. \code{"59/NRSITZ"}).
\item \code{legis_period} (character): Legislative period code (e.g. \code{"XXVIII"}).
\item \code{meeting_type} (character): Meeting type label (e.g. \code{"Plenarsitzung"}).
\item \code{resolution_top} (character): Agenda item label (e.g. \code{"TOP 1"}).
\item \code{resolution_title} (character): Agenda item title.
\item \code{resolution_url} (character): Relative URL to the main document.
\item \code{resolution_citation} (character): Citation of the main document (e.g. \code{"319 d.B."}).
}

If \code{details_on = "timeline"}:
\itemize{
\item \code{meeting_url} (character): The URL used to fetch the data.
\item \code{meeting_title} (character): Full title of the meeting.
\item \code{meeting_citation} (character): Citation reference (e.g. \code{"59/NRSITZ"}).
\item \code{legis_period} (character): Legislative period code (e.g. \code{"XXVIII"}).
\item \code{meeting_type} (character): Meeting type label (e.g. \code{"Plenarsitzung"}).
\item \code{stage_date} (character): Date of the event (\code{"DD.MM.YYYY"}), or \code{NA} for
agenda-item rows. The Schriftführer row (no text) is excluded.
\item \code{agenda_item} (character): Agenda item label (e.g. \code{"TOP 1"}) for TOP rows,
or \code{NA} for dated rows.
\item \code{stage_text} (character): Plain-text description of the event (HTML stripped).
\item \code{statements} (list): For "Wortmeldungen in der Debatte" rows, a nested tibble
with one row per speaker: \code{speaker_name} (character), \code{speaker_url} (character),
\code{wm_type} (character, e.g. \code{"Pro"}, \code{"Contra"}), \code{protocol_ref} (character,
e.g. \code{"RN/4"}), \code{protocol_url} (character). \code{NULL} for all other rows.
\item \code{stage_fsth_url} (character): URL to the stenographic protocol reference, or \code{NA}.
\item \code{stage_fsth_title} (character): Title of the stenographic protocol reference, or \code{NA}.
}
}
\description{
Retrieves detailed information about a specific plenary meeting from the
Austrian Parliament website. The function scrapes the embedded JavaScript
data from the meeting's detail page.

Supply either \code{url} \strong{or} the combination of \code{institution},
\code{legis_period}, and \code{meeting_number} — not both.
}
\examples{
\dontrun{
# Via URL — meeting metadata (default)
get_plenary_meeting_details(
  url = "https://www.parlament.gv.at/gegenstand/XXVIII/NRSITZ/50?selectedStage=100"
)

# Via structured arguments — numeric legis_period
get_plenary_meeting_details(institution = "NR", legis_period = 28, meeting_number = 50)

# Via structured arguments — Roman numeral legis_period, speakers mode
get_plenary_meeting_details(
  institution = "NR", legis_period = "XXVIII", meeting_number = 50,
  details_on = "speakers", echo = TRUE
)

# Via URL — speaker list with timing information
get_plenary_meeting_details(
  url = "https://www.parlament.gv.at/gegenstand/XXVIII/NRSITZ/50",
  details_on = "speakers"
)
}

}
\seealso{
\itemize{
\item \code{\link[=get_plenary_meetings]{get_plenary_meetings()}} for retrieving meeting URLs.
}
}
