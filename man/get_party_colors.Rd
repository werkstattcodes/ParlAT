% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_party_colors.R
\name{get_party_colors}
\alias{get_party_colors}
\title{Get colors for Austrian political parties}
\usage{
get_party_colors(
  parties = NULL,
  legis_period = NULL,
  output = c("vector", "tibble"),
  unmatched = c("NA", "error")
)
}
\arguments{
\item{parties}{Character vector of party or parliamentary group names,
abbreviations, or codes. If \code{NULL}, returns the default plotting palette.}

\item{legis_period}{Optional legislative period. If supplied, the historical
color for the Austrian People's Party (\code{"ÖVP"}) is returned as \code{"black"}
before legislative period 26 and as the modern palette color from period 26
onward. May be length 1 or the same length as \code{parties}.}

\item{output}{Character string. \code{"vector"} returns a named character vector;
\code{"tibble"} returns a tibble with party names and colors.}

\item{unmatched}{Character string. \code{"NA"} returns \code{NA} for unmatched inputs;
\code{"error"} throws an error if any input cannot be matched.}
}
\value{
A named character vector by default. With \code{output = "tibble"}, a
tibble with columns \code{party}, \code{canonical_party}, and \code{color}.
}
\description{
\code{get_party_colors()} returns a named vector or tibble of colors for Austrian
political parties and parliamentary groups. It accepts common party codes,
short parliamentary abbreviations, and full party or parliamentary group
names.
}
\examples{
get_party_colors()
get_party_colors(c("SPÖ", "ÖVP", "FPÖ"))
get_party_colors(c("S", "V", "F"))
get_party_colors(c("ÖVP", "ÖVP"), legis_period = c(25, 26))

# Display the default plotting palette.
party_colors <- get_party_colors(output = "tibble")
party_colors$party <- factor(
  party_colors$party,
  levels = rev(party_colors$party)
)

if (requireNamespace("ggplot2", quietly = TRUE)) {
  ggplot2::ggplot(
    party_colors,
    ggplot2::aes(x = party, y = 1, fill = color)
  ) +
    ggplot2::geom_col(width = 0.8) +
    ggplot2::scale_fill_identity() +
    ggplot2::coord_flip() +
    ggplot2::labs(
      title = "Default party color palette",
      x = NULL,
      y = NULL
    ) +
    ggplot2::theme_minimal() +
    ggplot2::theme(
      axis.text.x = ggplot2::element_blank(),
      axis.ticks.x = ggplot2::element_blank(),
      panel.grid = ggplot2::element_blank()
    )
}

# The ÖVP color changed from black to turquoise in legislative period 26.
oevp_data <- data.frame(
  legis_period = c(25, 26),
  party = c("ÖVP", "ÖVP"),
  value = c(1, 1)
)
oevp_data$party_color <- get_party_colors(
  parties = oevp_data$party,
  legis_period = oevp_data$legis_period
)

if (requireNamespace("ggplot2", quietly = TRUE)) {
  ggplot2::ggplot(
    oevp_data,
    ggplot2::aes(
      x = factor(legis_period),
      y = value,
      fill = party_color
    )
  ) +
    ggplot2::geom_col(width = 0.7) +
    ggplot2::scale_fill_identity() +
    ggplot2::labs(
      title = "Change in the ÖVP party color",
      subtitle = paste(
        "Black before legislative period 26,",
        "turquoise from period 26"
      ),
      x = "Legislative period",
      y = NULL
    ) +
    ggplot2::theme_minimal() +
    ggplot2::theme(
      axis.text.y = ggplot2::element_blank(),
      axis.ticks.y = ggplot2::element_blank()
    )
}
}
