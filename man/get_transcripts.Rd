% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_protocols.R
\name{get_transcripts}
\alias{get_transcripts}
\title{Retrieve Transcripts from the Austrian Parliament API}
\usage{
get_transcripts(
  search_string = NULL,
  legis_period = NULL,
  session_type = NULL,
  date_start = NULL,
  date_end = NULL,
  echo = TRUE
)
}
\arguments{
\item{search_string}{Optional character string to filter transcripts by keywords. Defaults to NULL.}

\item{legis_period}{Optional numeric value representing the legislative period. This value is converted
to its Roman numeral representation internally. Defaults to NULL.}

\item{session_type}{Optional character string specifying the type of session (e.g., "NRSITZ", "BRSITZ",
"USA", etc.). Defaults to NULL.}

\item{date_start}{Optional start date (as character or Date) for filtering transcripts. Defaults to NULL.}

\item{date_end}{Optional end date (as character or Date) for filtering transcripts. Defaults to NULL.}

\item{echo}{Logical flag to indicate whether to print details of the request process. Defaults to TRUE.}
}
\value{
An object containing the API response, which typically includes the transcript data in JSON format.
}
\description{
This function sends a POST request to the Austrian Parliament's API to retrieve transcript data
based on the provided filters such as search string, legislative period, session type, and date range.
}
\details{
\subsection{Session Type ('Art der Sitzung')}{
\itemize{
\item NRSITZ: Nationalrat - Plenarsitzungen (National Council - Plenary sessions)
\item BRSITZ: Bundesrat - Plenarsitzungen (Federal Council - Plenary sessions)
\item USA: Untersuchungsausschüsse (Inquiry Committees)
\item ENQ: Enqueten und Enquete-Kommissionen (Inquiries and Inquiry Commissions)
\item BVSITZ: Bundesversammlung (Federal Assembly)
\item AUS: Ausschüsse (Committees)
\item EU: EU-Ausschüsse (EU Committees)
\item GFT: Gedenk-, Fest- und Trauersitzungen (Memorial, Celebratory, and Condolence Sessions)
\item PARL: Jugend- und Lehrlingsparlament (Youth and Apprentice Parliament)
}
}
}
\examples{
\dontrun{
  # Retrieve all available transcripts with default filters.
  get_transcripts()

  # Retrieve transcripts using a search string and specifying a legislative period.
  get_transcripts(search_string = "gesundheit", legis_period = 28, session_type = "NRSITZ",
                date_start = "2024-01-01", date_end = NULL)
}
}
