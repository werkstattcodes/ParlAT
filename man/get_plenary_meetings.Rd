% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_plenary_meetings.R
\name{get_plenary_meetings}
\alias{get_plenary_meetings}
\title{Get Data on Plenary Meetings of the Austrian Parliament}
\usage{
get_plenary_meetings(
  institution = NULL,
  legis_period = NULL,
  meeting_and_activities = NULL,
  session_type = NULL,
  meeting_type = NULL,
  echo = FALSE
)
}
\arguments{
\item{institution}{A character string specifying the institution. "BR" (Bundesrat/Federal Council), "NR" (Nationalrat/National Council), or "BV" (Bundesversammlung/Federal Assembly).}

\item{legis_period}{Numeric value or vector specifying the legislative period(s). Can also be NULL to retrieve all periods from 20th onwards. \strong{Must be NULL when institution is "BV"} (Bundesversammlung does not use legislative periods).}

\item{meeting_and_activities}{A character string. One of 'meetings' or 'activities'. 'meetings' returns plenary meeting entries; 'activities' returns parliamentary items submitted or acted upon in meetings. Not applicable when institution is "BV" (Bundesversammlung); must be NULL for BV institution.}

\item{session_type}{A character string or vector. Filter by meeting period type. Permissible values: \code{"N"} (Ordentliche Tagung / Ordinary meeting period). Can be NULL to retrieve all meeting period types. Not applicable when institution is "BV".}

\item{meeting_type}{A character string or vector. Filter by sitting type. Permissible values: \code{"S"} (Sitzung / Regular sitting), \code{"SON"} (Sondersitzung / Special sitting), \code{"ZW"} (Zuweisungssitzung / Assignment sitting). Can be NULL to retrieve all sitting types. Only applicable when \code{meeting_and_activities = "meetings"}.}

\item{echo}{Logical. If \code{TRUE}, prints the API request body parameters and the number of results. Default is \code{FALSE}.}
}
\value{
A data frame containing plenary meeting details, or NULL if no results found. The structure depends on the \code{meeting_and_activities} parameter:

If \emph{\code{meeting_and_activities = "meetings"}}:
\itemize{
\item \code{institution}: parliamentary institution (e.g., "NR", "BR")
\item \code{legis_period}: legislative period (not returned if institution is 'BV')
\item \code{date}: date of the meeting
\item \code{meeting_number}: number of the meeting
\item \code{meeting_url}: URL to the meeting page
\item \code{meeting_type}: sitting type abbreviation (e.g., "S", "SON", "ZW")
\item \code{meeting_title}: full title of the meeting
\item \code{session_type}: meeting period type (e.g., "N" for Ordentliche Tagung)
\item \code{agenda_url_html}: URL to the agenda in HTML format (NA if not yet published)
\item \code{agenda_url_pdf}: URL to the agenda in PDF format (NA if not yet published)
}

If \emph{\code{meeting_and_activities = "activities"}}:
\itemize{
\item \code{institution}: parliamentary institution
\item \code{legis_period}: legislative period
\item \code{date}: date of the activity
\item \code{title}: title of the parliamentary item
\item \code{url_item}: URL to the parliamentary item
\item \code{meeting_number}: number of the meeting in which the item appeared
\item \code{url_meeting}: URL to the meeting
\item \code{session_type}: meeting period type of the meeting
\item \code{activity_type}: type of activity (e.g., "Sonstiges")
\item \code{doc_type}: document type description
\item \code{citation}: item citation string
}

For \emph{BV (Bundesversammlung) institution}:
\itemize{
\item Returns basic meeting information without activity filtering
\item Does NOT include \code{legis_period} column (not applicable for Federal Assembly)
\item Columns returned: \code{institution}, \code{date}, \code{meeting_number}, \code{meeting_url}, \code{meeting_type}, \code{meeting_title}, \code{session_type}, \code{agenda_url_html}, \code{agenda_url_pdf}
}
}
\description{
Retrieves information about plenary meetings from the Austrian Parliament's API (see \if{html}{\out{<a href="https://www.parlament.gv.at/recherchieren/plenarsitzungen/index.html" target="_blank" rel="noopener">}}here\if{html}{\out{</a>}}).
Data available from 20th legislative period onwards.
}
\examples{
\dontrun{
# Basic usage: meetings of the National Council for legislative period 28
meetings <- get_plenary_meetings(
  institution = "NR",
  legis_period = 28,
  meeting_and_activities = "meetings"
)

# Parliamentary activities during meetings
get_plenary_meetings(
  institution = "NR",
  legis_period = 27,
  meeting_and_activities = "activities"
)

# Federal Council meetings
get_plenary_meetings(
  institution = "BR",
  legis_period = 28,
  meeting_and_activities = "meetings"
)

# Multiple legislative periods
get_plenary_meetings(
  institution = "NR",
  legis_period = c(26, 27),
  meeting_and_activities = "meetings"
)

# All periods from 20th onwards (NULL legis_period)
get_plenary_meetings(
  institution = "NR",
  legis_period = NULL,
  meeting_and_activities = "meetings"
)

# Federal Assembly (no legis_period, no meeting_and_activities)
get_plenary_meetings(institution = "BV", legis_period = NULL)
}

}
