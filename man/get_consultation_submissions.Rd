% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_consultation_submissions.R
\name{get_consultation_submissions}
\alias{get_consultation_submissions}
\title{Get Consultation Submissions for a Draft or Bill}
\usage{
get_consultation_submissions(item_url, echo = TRUE)
}
\arguments{
\item{item_url}{Character scalar. Full Parliament URL or relative path of
the parent item. Supported object types are ministerial drafts (\code{ME}),
parliamentary papers such as government bills (\code{I}), and motions (\code{A}).
Query strings, fragments and a trailing slash are ignored.}

\item{echo}{Logical scalar. If \code{TRUE}, display the parent item's website URL,
pagination progress and the number of submissions returned.}
}
\value{
An ungrouped tibble with one row per submission and these columns:
\itemize{
\item \code{legis_period}: Legislative period (character).
\item \code{date}: Submission date (\code{Date}).
\item \code{submission_id}: Citation, e.g. \code{624/SN-44/ME} (character).
\item \code{submission_code}: Submission type, e.g. \code{SNME} or \code{SN} (character).
\item \code{author}: Published person or organisation name (character).
\item \code{submission_url}: Full URL of the submission's detail page (character).
\item \code{support_n}: Number of supports (numeric).
\item \code{item_id}: Parent reference as returned by the API (character).
\item \code{item_code}: Parent object type, e.g. \code{ME} or \code{I} (character).
\item \code{item_url}: Full URL of the parent item (character).
}

No results produce a zero-row tibble with the same columns and types.
}
\description{
Retrieve references to consultation submissions (Stellungnahmen im
Begutachtungsverfahren), called "opinions" on Parliament's English website.
These are written contributions by individuals or organisations concerning
one ministerial draft or parliamentary bill.
}
\details{
The function queries the parent-specific consultation filter and retrieves
all pages. It returns references and metadata, without downloading submission
texts, PDFs or individual detail pages. Results follow the API's date order
(newest first). Non-public submissions remain in the result, with unavailable
authors and links recorded as \code{NA}. A submission URL is returned only when
the API supplies a link; internal identifiers are not used to invent links.

The object type is taken from the parent URL. For example, a government
bill may use \code{I} in its URL even though its document type is \code{RV}.
If an HTTP request fails, pagination changes during retrieval, or the API
returns an inconsistent result, the function errors rather than returning
an incomplete table. Retry the call if the underlying results have changed.
}
\examples{
\donttest{
submissions <- get_consultation_submissions("/gegenstand/XXVIII/ME/44")
submissions |>
  dplyr::select(submission_id, author, submission_url)

get_consultation_submissions("/gegenstand/XXVIII/I/405", echo = FALSE)

# Combine submissions for several parent items in one flat table.
c("/gegenstand/XXVIII/ME/44", "/gegenstand/XXVIII/I/405") |>
  purrr::map(\(url) get_consultation_submissions(url, echo = FALSE)) |>
  purrr::list_rbind()
}
}
\seealso{
\code{\link[=get_participation]{get_participation()}} for searching participation items and their
submission counts; \code{\link[=get_item_details]{get_item_details()}} for a public submission's details.
}
