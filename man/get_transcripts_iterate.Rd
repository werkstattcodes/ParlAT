% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_transcripts_iterate.R
\name{get_transcripts_iterate}
\alias{get_transcripts_iterate}
\title{Retrieve Transcripts from the Austrian Parliament API}
\usage{
get_transcripts_iterate(
  search_string = NULL,
  legis_period = NULL,
  session_type = NULL,
  date_start = NULL,
  date_end = NULL,
  echo = TRUE
)
}
\arguments{
\item{search_string}{Optional character string to filter transcripts by keywords. Defaults to NULL.}

\item{legis_period}{Legislative period(s). Default NULL queries for all legislative periods. Accepts numeric, character or roman numerals in character format as well as "KN" (Konstituierende Nationalversammlung) and "PN" (Provisorische Nationalversammlung).}

\item{session_type}{Optional character string specifying the type of session (e.g., "NRSITZ", "BRSITZ",
"USA", etc.). Defaults to NULL. See Details for complete list of session types.}

\item{date_start}{Optional start date for filtering transcripts. Defaults to NULL.}

\item{date_end}{Optional end date for filtering transcripts. Defaults to NULL.}

\item{echo}{Logical flag to indicate whether to print details of the request process. Defaults to TRUE.}
}
\value{
A tibble containing transcript data with the following columns:
\describe{
\item{date}{Date of the session}
\item{session_url}{URL to the session page}
\item{legis_period}{Legislative period}
\item{session_type}{Type of session}
\item{session_number}{Session number/citation}
\item{session}{Session description}
\item{session_transcript_html}{URL to HTML transcript (if available)}
\item{session_transcript_pdf}{URL to PDF transcript (if available)}
}
}
\description{
\code{get_transcripts()} retrieves the transcripts of parliamentary sessions via Parliament's API (see \if{html}{\out{<a href="https://www.parlament.gv.at/recherchieren/protokolle/index.html" target="_blank" rel="noopener">}}here.\if{html}{\out{</a>}})
}
\details{
\subsection{Session Type ('Art der Sitzung')}{
\itemize{
\item NRSITZ: Nationalrat - Plenarsitzungen (National Council - Plenary sessions)
\item BRSITZ: Bundesrat - Plenarsitzungen (Federal Council - Plenary sessions)
\item USA: Untersuchungsausschüsse (Inquiry Committees)
\item ENQ: Enqueten und Enquete-Kommissionen (Inquiries and Inquiry Commissions)
\item BVSITZ: Bundesversammlung (Federal Assembly)
\item AUS: Ausschüsse (Committees)
\item EU: EU-Ausschüsse (EU Committees)
\item GFT: Gedenk-, Fest- und Trauersitzungen (Memorial, Celebratory, and Condolence Sessions)
\item PARL: Jugend- und Lehrlingsparlament (Youth and Apprentice Parliament)
\item VER: Veranstaltungen (Events)
}
}
}
\examples{
\dontrun{
  # Retrieve all available transcripts with default filters.
  get_transcripts()

  # Retrieve transcripts using a search string and specifying a legislative period.
  get_transcripts(search_string = "gesundheit", legis_period = 28, session_type = "NRSITZ",
                date_start = "01-01-2024", date_end = NULL)
}
}
