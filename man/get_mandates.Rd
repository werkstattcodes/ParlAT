% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_mandates.R
\name{get_mandates}
\alias{get_mandates}
\title{Get mandates}
\usage{
get_mandates(name = NULL, pad_intern = NULL, institution = NULL, date = NULL)
}
\arguments{
\item{pad_intern}{Personal identfication number of person(s). Has to be NULL if names are provided}

\item{institution}{Chamber of Parliament. "NR" (Nationalrat), "BR" (Bundesrat), "KN" (Konstituierende Nationalversammlung),
or "PN" (Provisorische Nationalversammlung). NULL covers all institutions. Note that e.g. "NR" does not only return MP's mandates,
but also presidents of the National Council, Secretaries ("Schriftführer"), and Regulators ("Ordner"). The equivalent applies to other
the chambers as well.}

\item{date}{Date to filter mandates (dmy format).}

\item{names}{A character vector of name(s). First name followed by given name. Only considered if no pad_intern(s) provided.}
}
\value{
A dataframe with the following columns:
\itemize{
\item \code{pad_intern}: Person's unique identification number
\item \code{name}: Name of the person
\item \code{position_text}: Full description of the position
\item \code{position_code}: Code for the position type
\item \code{position_name}: Name of the position/function
\item \code{position_date_start}: Start date of the position (Date)
\item \code{position_date_end}: End date of the position (Date, NA if currently active)
\item \code{position_active}: Logical indicating if the position is currently active
\item \code{parl_group}: Parliamentary group affiliation
\item \code{party}: Political party code
\item \code{party_name}: Full name of the political party
\item \code{substitute}: Information about substitute status
\item \code{electoral_district_region_code}: Electoral district region code
\item \code{electoral_district_region}: Electoral district region name
\item \code{legis_period}: Legislative period(s) (list-column)
\item \code{url_biography}: URL to the person's biography page
}
}
\description{
Takes one or multiple names or pad_interns as input and returns a dataframe
with all their past and present mandates. Mandates can be limited to a specific date
or institution. Mandates cover memberships in Parliament, but also in the executive (e.g. Bundeskanzler/Chancellor).

Note that a single row in the returned dataframe can represent e.g. multiple mandates in e.g. the Nationalrat if they were consecutively held.

The function partly mimics the behavior of the 'Personensuche' on the website
of the Parliament (\if{html}{\out{<a href="https://www.parlament.gv.at/recherchieren/personen/" target="_blank">}}here\if{html}{\out{</a>}}).
}
\details{
\subsection{Names: The API will always return the latest name of an MP, even if the MP had a different name at a previous point in time.}{

See examples.
}
}
\examples{
\dontrun{
  get_mandates(c("Elisabeth Götze", "Sebastian Kurz"))
  get_mandates(c("Pia Philippa Strache")) #returns results with latest name (Beck)

  get_names(pad_intern="83124") #Michael Pöck changed name to Michael Bernhard.
  get_mandates(name="Michael Pöck") #Query for Micheal Pöck returns all results under the name Michael Bernhard, even for periods where Michael Pöck was still valid.
  get_mandate(name="Michael Bernhard") #Query for Michael Bernhard returns all results, including for those with the name Michael Pöck.
}

}
\seealso{
\code{\link[=get_names]{get_names()}}, \code{\link[=get_pad_intern]{get_pad_intern()}}
}
